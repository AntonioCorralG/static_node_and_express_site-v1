extends layout.pug

block content

  article.portfolio-intro
    h1 My Work:
    p The purpose of the portofolio below is to showcase a multitude of skills related to solving real world problems using critical thinking skills. 
  article.portfolio-project-thumbs
    .projects-box.box.grid
       each project in projects
        .cell
          a(href= `/projects/${project.id}`)
            img.thumbnail(src=`${project.image_urls[1]}`, alt=`${project.project_name}`)
            h5=`${project.project_name}` 

            
